<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Byte Insider - Tech Blogging Site</title>
    <link rel="icon" href="/images/byte.png" type="image/x-icon" />
    <link rel="stylesheet" href="/stylesheets/home.css" />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css"
      integrity="sha512-SnH5WK+bZxgPHs44uWIX+LLJAJ9/2PkPKZ5QiAj6Ta86w+fsb2TkcmfRyVX3pBnMFcV7oQPJkl9QevSCWr3W6A=="
      crossorigin="anonymous"
      referrerpolicy="no-referrer"
    />
  </head>
  <body>
    <div class="site">
      <%- include("../views/partials/navabar.ejs") %>
      <main>
        <div class="container">
          <section class="main-content">
            <h2 class="center">Our Blog</h2>
            <h1 class="center">Innovation & Ideas</h1>
            <p class="center">
              Discover the future of innovation, unravel the mysteries of
              technology, and explore the cutting-edge trends shaping our world
            </p>
            <div class="content-grid">
              <% if (Blog1) { %>
              <div class="blog-post">
                <img src="<%= Blog1.coverPhoto %>" alt="<%= Blog1.title %>" />
                <div class="post-content">
                  <span class="category"
                    ><%= Blog1.author.Username %> &middot; <%=
                    Blog1.createdAt.toDateString() %>
                  </span>
                  <h2><%= Blog1.title %></h2>
                  <p><%= Blog1.content.split(' ').slice(0, 30).join(' ') %></p>
                  <span class="tags">
                    <a class="tags-link" href="/blog/<%= Blog1._id %>">Read</a>
                    <% if (Blog1.isLiked) { %>
                    <a class="like" href="/unlike/<%= Blog1._id %>">
                      <i class="fa-solid fa-heart"></i>
                    </a>
                    <% } else { %>
                    <a class="notlike" href="/like/<%= Blog1._id %>">
                      <i class="fa-solid fa-heart"></i>
                    </a>
                    <% } %>
                  </span>
                </div>
              </div>
              <% } %>
              <aside class="sidebar">
                <h3>Top Stories</h3>
                <ul>
                  <% Blog3.forEach(blog => { %>
                  <li>
                    <img src="<%= blog.coverPhoto %>" alt="<%= blog.title %>" />
                    <div class="story-info">
                      <h4><%= blog.title %></h4>
                      <p>
                        <%= blog.author.Username %> • <%=
                        blog.createdAt.toDateString() %>
                      </p>
                      <span class="top-stories-read">
                        <a class="top-link" href="/blog/<%= blog._id %>"
                          >Read</a
                        >
                        <% if (blog.isLiked) { %>
                        <a class="like" href="/unlike/<%= blog._id %>">
                          <i class="fa-solid fa-heart"></i>
                        </a>
                        <% } else { %>
                        <a class="notlike" href="/like/<%= blog._id %>">
                          <i class="fa-solid fa-heart"></i>
                        </a>
                        <% } %>
                      </span>
                    </div>
                  </li>
                  <% }) %>
                </ul>
              </aside>
            </div>
            <div class="additional-posts">
              <% Blogs.forEach(blog => { %>
              <div class="post">
                <img src="<%= blog.coverPhoto %>" alt="<%= blog.title %>" />
                <div class="post-content">
                  <span class="author">
                    <%= blog.author.Username %> • <%=
                    blog.createdAt.toDateString() %>
                  </span>
                  <h3><%= blog.title %></h3>
                  <p><%= blog.content.split(' ').slice(0, 30).join(' ') %></p>
                  <span class="tags">
                    <a class="tags-link" href="/blog/<%= blog._id %>">Read</a>
                    <% if (blog.isLiked) { %>
                    <a class="like" href="/unlike/<%= blog._id %>">
                      <i class="fa-solid fa-heart"></i>
                    </a>
                    <% } else { %>
                    <a class="notlike" href="/like/<%= blog._id %>">
                      <i class="fa-solid fa-heart"></i>
                    </a>
                    <% } %>
                  </span>
                </div>
              </div>
              <% }) %>
            </div>
          </section>
        </div>
      </main>
      <%- include("../views/partials/footer.ejs") %>
    </div>
    <script src="/javascripts/home.js"></script>
  </body>
</html>
